<div class="tab-pane fade" id="tab-planning">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="text-white mb-0">Analyses d’eau</h4>

        <a href="{{ path('app_evenement_new', { id: aquarium.id }) }}"
           class="btn btn-sm btn-outline-warning">
            <i class="bi bi-eyedropper me-1"></i> Ajouter un Événement
        </a>
    </div>
    <!-- BEGIN calendar -->
    <div class="calendar">
        <!-- BEGIN calendar-body -->
        <div class="calendar-body">
            <div data-scrollbar="true" data-height="100%">
                <div id="calendar"></div>
            </div>
        </div>
        <!-- ENG calendar-body -->
    </div>
    <!-- end calendar -->
</div>
